{
  // Logging configuration
  "Logging": {
    "LogLevel": {
      "Default": "Information", // Default log level is set to Information
      "Microsoft.AspNetCore": "Warning" // Log level for Microsoft.AspNetCore is set to Warning
    }
  },

  "AllowedHosts": "*", // All hosts are allowed to send requests to the application

  // Connection string for PostgreSQL database
  "ConnectionStrings": {
    "PostgreSQL": "Host=;Database=;Username=;Password=;"
  },

  "AWS": {
    // Access key for AWS
    "AccessKey": "",

    // Secret key for AWS
    "SecretKey": "",

    // AWS region
    "Region": "",

    // Name of the AWS S3 bucket
    "BucketName": ""
  }
}
